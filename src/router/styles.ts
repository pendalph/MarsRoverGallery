import { StyleSheet } from 'react-native';

import { mainDarkLighterColor } from '_app/constants';

export const styles = StyleSheet.create({
  activeContainer: {
    padding: 6,
    backgroundColor: mainDarkLighterColor,
    borderRadius: 8,
  },
});
