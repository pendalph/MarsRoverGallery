import React, { FC } from 'react';
import { Text, View } from 'react-native';

export const EventsScreen: FC = (): JSX.Element => {
  return (
    <View style={{ flex: 1 }}>
      <Text>EventsScreen</Text>
    </View>
  );
};
