import React, { FC } from 'react';
import { Text, View } from 'react-native';

export const FavoritesScreen: FC = (): JSX.Element => {
  return (
    <View style={{ flex: 1 }}>
      <Text>FavoritesScreen</Text>
    </View>
  );
};
