import React, { FC } from 'react';
import { Text, View } from 'react-native';

export const SettingsScreen: FC = (): JSX.Element => {
  return (
    <View style={{ flex: 1 }}>
      <Text>SettingsScreen</Text>
    </View>
  );
};
